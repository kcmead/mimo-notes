activeUsers.push("ted"); // - adds ted to the array

activeUsers.length; // gets the # of entries in array

activeUsers.pop();  // removes last item in array

consttasks =["dishes","windows","vacuum"];
const length = tasks.length; // gets the length of array;
console.log(length); // pushes array to console

